import React from 'react';
import LoginOrRegisterForm from './LoginOrRegisterForm';

const ResetPassword = () => {
  return <LoginOrRegisterForm loginOrRegister={'reset'} />;
};

export default ResetPassword;
